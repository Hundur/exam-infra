platform: linux

image_resource:
  type: docker-image
  source:
    repository: psharkey/heroku-cli
    tag: "latest"

inputs:
- name: source

params:
    heroku_email:
    heroku_api_token:
    app_name:

run:
  path: sh
  dir: source
  args:
    - -exc
    - |
      heroku addons:add hostedgraphite --app hundur-app-ci
